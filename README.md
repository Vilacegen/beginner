# Personal Profile Page - Beginner HTML Project

A simple, single-page personal profile built with HTML to demonstrate foundational web development concepts.

## 📋 Project Goal

This project showcases understanding of **basic HTML structure** and **common semantic tags** used in web development. It's designed as a first step in learning how to build proper, accessible web pages.

## ✨ Features

### Core HTML Concepts Demonstrated

- ✅ **Document Structure**: Proper use of `<!DOCTYPE html>`, `<html>`, `<head>`, and `<body>` tags
- ✅ **Headings**: Multiple heading levels (`<h1>` – `<h3>`) for content hierarchy
- ✅ **Text Content**: Paragraphs (`<p>`) for meaningful text blocks
- ✅ **Links**: Multiple hyperlinks (`<a>`) to external profiles (GitHub, LinkedIn, etc.)
- ✅ **Images**: Local or online images (`<img>`) with proper attributes
- ✅ **Lists**: Both unordered (`<ul>`) and ordered (`<ol>`) lists
- ✅ **Text Formatting**: Text emphasis using `<strong>`, `<em>`, `<br>`, and `<hr>` for visual separation
- ✅ **Metadata**: Proper `<title>` tag and `<meta charset="UTF-8">` declaration
- ✅ **Semantic HTML**: Uses semantic tags like `<header>`, `<main>`, and `<footer>` for better structure and accessibility

## 📁 Project Structure
